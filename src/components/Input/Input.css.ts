import { style } from '@vanilla-extract/css';

export const createBoardInput = style({
  margin: '10px 10px 0 0',
  padding: '4px 8px',
});

export const inputWrapper = style({
  display: 'flex',
  flexDirection: 'column',
  marginBottom: '16px',
});
